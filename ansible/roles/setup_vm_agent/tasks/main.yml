- name: Install qemu-guest-agent
  apt:
    name: qemu-guest-agent
    state: present
    update_cache: yes
  become: true

- name: Ensure qemu-guest-agent is running
  systemd:
    name: qemu-guest-agent
    enabled: yes
    state: started
  become: true